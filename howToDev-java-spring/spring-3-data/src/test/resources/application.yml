---
spring:
 datasource:
  url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
  driver-class-name: org.h2.Driver
 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
---
logging:
  level:
   org.hibernate.orm.jdbc.bind: trace #Enable bound vars in sql queries
---